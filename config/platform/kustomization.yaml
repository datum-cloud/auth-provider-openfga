apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Platform-level infrastructure services
# These are cluster-wide services that support multiple applications

resources:
  # cert-manager namespace and components
  - namespace.yaml
  - jetstack-helmrepository.yaml
  - cert-manager-helmrelease.yaml
  - cert-manager-csi-driver-helmrelease.yaml
# Platform services typically deploy to their own namespaces
# cert-manager creates its own namespace via the HelmRelease
